import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Sibyl Oracle — AI Political Predictions",
  description:
    "Real-time AI-powered political predictions with personal impact analysis. See how election outcomes affect your career and industry.",
  openGraph: {
    title: "Sibyl Oracle — AI Political Predictions",
    description:
      "Real-time AI-powered election predictions, updated every 15 minutes.",
    type: "website",
    images: [{ url: "/api/og", width: 1200, height: 630 }],
  },
  twitter: {
    card: "summary_large_image",
    title: "Sibyl Oracle — AI Political Predictions",
    description:
      "Real-time AI-powered election predictions, updated every 15 minutes.",
    images: ["/api/og"],
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
